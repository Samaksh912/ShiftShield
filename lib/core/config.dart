class AppConfig {
  // Android emulator reaches host machine localhost via 10.0.2.2
  // For a real device on same WiFi, replace with your machine's LAN IP
  static const String baseUrl = 'http://10.0.2.2:3000';
}
